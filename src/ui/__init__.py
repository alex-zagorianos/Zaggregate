"""UI support package: a clean light/modern Tk theme, reusable widget helpers,
the in-app Guide + Help dialogs, and the first-run Setup wizard.

Kept separate from gui.py so the 2,100-line window module stays focused on tab
behavior and the look/onboarding live in small, testable units."""
