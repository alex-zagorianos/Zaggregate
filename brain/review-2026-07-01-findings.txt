COUNTS: {'total_raw': 38, 'kept': 38, 'critical': 7, 'major': 21} | kept: 38
#1 [CRITICAL/reach] (reach) No in-app onboarding for the free keys that gate most job reach (Adzuna/USAJobs/Brave)  <CONFIRMED effort=M>  @ ui/setup_wizard.py:1
    IMPACT: A non-technical user who completes the wizard (goal 3) ends up running on only the key-less single-feed boards (all tech-audience) plus the eng-only static registry — i.e. near-zero reach for any non-engineering field, with no GUI error. This is the actual mechanism behind tonight's measured '18 job
    REC: Add an API-Keys step to setup_wizard.py (and settings.py `_KEY_FILES`) for adzuna_app_id/adzuna_app_key, usajobs_api_key/usajobs_user_agent, and brave_search_api_key, each with a 'free, 2 min, register here' link, plus a visible GUI warning banner when a source silently degrades.
#2 [CRITICAL/bug] (bugs) Every diagnostic print() in the ingest pipeline is discarded on the actual scheduled/headless run — 0-result sources are indistinguishable from broken sources  <CONFIRMED effort=S>  @ search/search_engine.py:167
    IMPACT: On the real deployment path (Windows Task Scheduler, the app's documented daily-run mechanism), an API key expiring, a scraper's JSON schema changing, a 429/500 storm, etc. produces silent, permanent, unrecoverable-from-logs source failure — exactly the ambiguity tonight's live test had to resolve b
    REC: Minimum fix (S): redirect the scheduled task's stdout/stderr to the per-project daily_run.log in scripts/setup_schedule.py's `/TR` string (`... && py daily_run.py --project {slug} >> "{log_path}" 2>&1`). Better (M): thread a callback/logger through SearchEngine/CareersClient instead of bare print(), and persist a `source_errors: {source: 
#3 [CRITICAL/bug] (scoring) Deterministic scorer (the score users actually see) has zero exec/seniority awareness — session-22 exec-fit logic never reaches it  <CONFIRMED effort=M>  @ match/scorer.py:242
    IMPACT: The 'exec-fit lift' the user believes was built (session 22) literally does not exist in the code path that produced the measured scores (CMIO=58, VP Quality=28, Senior Manager=84/80). Every default run — CLI search, daily_run.py (the scheduled/automated path), and the plain inbox — ranks purely on 
    REC: Wire rubric/gate signals into match/scorer.score_job directly: compute facts (match.facts.facts_for) and rubric (match.rubric.build_rubric) once per batch in score_jobs, and add a real additive/multiplicative 'target-level match' component (or bonus) to the deterministic score — not just to the optional AI prompt. At minimum, reuse `_EXEC
#4 [CRITICAL/bug] (scoring) Title-scoring partial credit and the title-miss penalty use inconsistent term sets — real target titles get hammered, unrelated titles score 100  <CONFIRMED effort=M>  @ match/scorer.py:99
    IMPACT: This is the direct mechanism behind the measured ranking inversion (PwC Senior Manager 84/80 vs CMIO 58 vs VP Quality 28): any title that happens to share two long words with a keyword phrase (even a non-exec IC/manager consulting role) is treated as a perfect match, while genuinely on-target exec t
    REC: Make the two checks consistent: either (a) compute the miss penalty from the SAME term set as `t` (so a t=1.0 near-match and a title_hit-true full-match are never inconsistent), or (b) drop the separate binary title_hit/miss-penalty entirely and rely solely on the already-graded `t` in the weighted score (it already penalizes proportional
#5 [CRITICAL/agnostic] (agnostic) Jobicy client hardcodes 'engineering' as the single server-side industry filter  <CONFIRMED effort=S>  @ config.py:184
    IMPACT: Same structural zero-reach bug as The Muse: a health/finance/trade seeker gets 0 from Jobicy no matter their keywords — confirmed by the measured 'jobicy 0' result. This is one of the 'remote-favoring persona structurally unserved' sources noted in the live test, and the root cause is a single hardc
    REC: Same fix pattern as The Muse: derive the Jobicy `industry` query param from the active project's industry (Jobicy's API supports categories like 'healthcare', 'business', 'finance', 'accounting'); default to 'engineering' only when industry is empty/eng-like.
#6 [CRITICAL/ux] (ux) Job-source API keys (Adzuna, USAJobs, Jooble, Careerjet, JSearch, Brave) have no in-app setup path  <CONFIRMED effort=M>  @ ui/settings.py:72
    IMPACT: For a non-technical user (dad), the only sources reachable without manually authoring a plaintext .env in the right folder are whatever ships pre-filled. This is the direct mechanism behind tonight's measured 18-jobs-total run: jooble/careerjet ('FREE-key aggregators... NOT configured') and the tech
    REC: Extend ui/settings.py's key store + 'Connect your AI' dialog into a 'Connect job sources' panel covering Adzuna/USAJobs/Jooble/Careerjet/JSearch/Brave, each with a short label, a 'Get a free key' link (webbrowser.open to the signup URL) and a one-click 'Test key' call; wire config.py's resolvers to also check SECRETS_DIR the way anthropic
#7 [CRITICAL/measurement] (measurement) Company-registry coverage tool requires the user to hand-build an independent second list; no automated/wired path exists, so a non-technical user can never get a trustworthy number  <CONFIRMED effort=M>  @ company_coverage.py:34
    IMPACT: Goal (4) 'a real way to measure reach' is unmet for both target users (Alex and Dad); nobody will run a manual CLI + self-sourced file, so the registry's completeness (4-of-273 health-tagged companies tonight, 2 of which are test fixtures) stays invisible until a live search fails.
    REC: Add a 'Registry reach' panel in gui.py that runs discover/cc_harvest.harvest_host_index for the active industry's ATS hosts WITHOUT merging into companies.json, and feeds it straight into coverage.registry_coverage.estimate_coverage_industry, rendering est.summary(). Makes the existing math self-service.
#8 [MAJOR/bug] (reach) TheMuse and Jobicy are hardcoded to fetch ONLY the Engineering category, server-side, before any keyword filtering — a free fix flagged 2 weeks ago and never applied  <CONFIRMED effort=S>  @ config.py:169
    IMPACT: Unlike the genuinely tech-audience boards (RemoteOK/Remotive/HN), TheMuse and Jobicy host real non-tech listings the app is actively refusing to request. Tonight's '0 results' from themuse/jobicy for dad's health search looks identical to the tech-only-board failures but has a different, trivially-f
    REC: Replace the constants with a per-project industry->category mapping (e.g. DEFAULT_INDUSTRY/config.industry -> ["Healthcare"] for Jobicy, ["Healthcare","Medical & Health"] for Muse), defaulting to today's engineering categories when industry is unset/eng-like (mirrors `is_eng_like` already in discover/enumerate.py).
#9 [MAJOR/reach] (reach) Jooble and Careerjet are structurally excluded from the automated daily pipeline regardless of configuration  <CONFIRMED effort=S>  @ config.py:209
    IMPACT: Measured findings recommend wiring jooble/careerjet as the fix for non-tech reach — but even after adding the free keys, the scheduled 07:30 Task-Scheduler autopilot (daily_run.py's own stated purpose) will never use them; the user would have to remember a manual CLI run daily, defeating automation.
    REC: Add opt-in gating for jooble/careerjet to the daily source list, e.g. `sources = [s for s in DAILY_SOURCES if cfg_sources.get(s, True)] + [s for s in ("jooble","careerjet") if cfg_sources.get(s)]`, so a user who configures a key can opt in without the jsearch/serpapi quota risk (which correctly stay excluded).
#10 [MAJOR/bug] (reach) Jooble and Careerjet clients hardcode single-page fetch — --max-pages is silently ignored  <CONFIRMED effort=S>  @ search/jooble_client.py:18
    IMPACT: Even after fixes #1/#3 land, Jooble/Careerjet reach caps at whatever a single default API page returns (~10-20 results) per keyword-location regardless of configured max_pages — for a broad recall keyword (the recommended fix for the narrow-keyword regression), fields like nursing/health informatics
    REC: Wire the `page` arg through to Jooble's `page` request field and Careerjet's `page` query param (both APIs support pagination), keeping the cache_key page-aware like adzuna_client.py.
#11 [MAJOR/reach] (reach) Remote coverage for non-tech fields is structurally near-zero: the 5 boards that aren't category-gated are tech-audience at the source  <CONFIRMED effort=M>  @ search/cli.py:20
    IMPACT: The 'remote-favoring non-tech seeker' persona (Dad, VP Health Informatics) has effectively no dedicated remote channel once the tech boards are discounted — remote reach collapses to whatever Adzuna/USAJobs/LinkedIn-guest surface with 'remote' in the location string, exactly matching tonight's 1
    REC: After fixing #2 (frees Jobicy/TheMuse for non-eng industries), evaluate one more general-purpose free remote source (e.g. We Work Remotely public feed); until then, document clearly that 'remote coverage' today is tech-only unless industry-scoped Jobicy/Muse are enabled.
#12 [MAJOR/reach] (reach) Live per-run company auto-discovery never targets enterprise ATS, inconsistent with the offline enumeration path that does  <CONFIRMED effort=S>  @ scrape/discoverer.py:15
    IMPACT: A user who never runs the manual `--discover-enterprise` maintenance command (most non-technical users won't) gets live discovery that's permanently blind to the platforms enterprise/health-system employers actually use, capping reach to startup/mid-size Greenhouse/Lever/Ashby companies even with Br
    REC: Extend `_ATS_SITES` with iCIMS/Taleo/SuccessFactors site: queries mirroring funnel.py's enterprise set, or have daily/GUI runs periodically invoke the enterprise funnel automatically (daily_run.py:185 already has an off-by-default `discover_on_daily` flag — surface it in onboarding).
#13 [MAJOR/bug] (bugs) Jobs with no URL are silently and permanently dropped at the inbox insert, even though several ATS scrapers can legitimately emit an empty URL  <CONFIRMED effort=M>  @ tracker/db.py:514
    IMPACT: Any posting from an Ashby/Lever/Recruitee/Rippling/Workable board that (even rarely) omits its URL field is invisible in the app forever — not deduped, not shown, not logged as lost. This is a real reach hole on exactly the ATS types the registry leans on most (goal #1), and it happens with zero use
    REC: In inbox_add_many, when normalize_url(j.url) is empty, fall back to `j.job_key` (already computed via coverage.entity for cross-source identity) as the uniqueness key instead of dropping the row outright, and widen the schema constraint accordingly (or add a nullable `dedup_key` column used when norm_url is blank). At minimum, count and l
#14 [MAJOR/bug] (bugs) URL-less duplicate detection drops location, silently merging distinct same-title postings at the same company into one  <CONFIRMED effort=S>  @ search/search_engine.py:205
    IMPACT: A company posting the same title in two different cities (a common real pattern — e.g. 'Director of Clinical Informatics' open in both Cincinnati and Remote) collapses into a single result whenever the URL is missing (see finding above for which scrapers can produce that). Concretely: two genuinely 
    REC: Include a location token (e.g. coverage.entity.location_token(normalize_location(job.location))) in `_keyless_key`, matching what `job_key` already does, so URL-less dedup only collapses postings that are actually the same city/remote bucket. One-line tuple extension plus a regression test asserting two same-title/same-company/different-c
#15 [MAJOR/measurement] (bugs) Bulk dataset-seeded registry entries can duplicate an already-registered board under a differently-cased slug, double-scraping the same employer and inflating the reach/coverage count  <CONFIRMED effort=S>  @ discover/dataset_seed.py:133
    IMPACT: A duplicate board entry means CareersClient (scrape/careers_client.py:101-127) scrapes the identical Greenhouse/Lever/Ashby board twice every run (wasted network/time under CAREERS_MAX_WORKERS), and `company_coverage.py`/`registry_history.py` — the tool this project explicitly built to answer 'are w
    REC: Lowercase/strip slugs at ingest in `_row_to_board` (dataset_seed.py) before they enter the boards dict, and change `save_companies`'/`get_registry`'s dedup/merge to key on `(ats_type, slug.lower())` in addition to name, so a bulk import can't silently double-register a board already in companies.json under different casing or a different 
#16 [MAJOR/scoring] (bugs) The deterministic local scorer has no seniority/target-role-alignment dimension, so it can't rank an exec's actual target level above an irrelevant keyword-matching title  <CONFIRMED effort=M>  @ match/scorer.py:302
    IMPACT: Two postings that both satisfy the (now-broadened) title keywords score identically on the `t` component regardless of role level — a 'Senior Manager, Financial Reporting' at a Big-4 firm and a 'VP Clinical Informatics' role both hit generic terms like 'manager'/'informatics' and tie on title, so th
    REC: Add a 6th scored component (or fold into the title weight) that checks the posting's detected seniority (reuse coverage.entity.normalize_title's seniority regex or match/facts.py's seniority extraction) against `rubric['seniority_target']`/`allow_management`, giving a boost when they align and a mild penalty when the posting is clearly be
#17 [MAJOR/agnostic] (scoring) Skill-overlap component (25% of score) silently goes neutral for any resume that doesn't use the literal 'TECHNICAL SKILLS' heading  <CONFIRMED effort=S>  @ resume/experience_parser.py:11
    IMPACT: For a non-engineering profile whose resume doesn't happen to use the exact heading 'TECHNICAL SKILLS'/'SKILLS', a quarter of the composite score's weight silently stops discriminating between jobs at all — every posting gets the same neutral 0.5 skill score, regardless of actual fit, without any vis
    REC: Add exec/health-IT-appropriate heading aliases ("CORE COMPETENCIES", "AREAS OF EXPERTISE", "KEY SKILLS"), and/or surface a GUI warning when skill_terms is empty so the user knows 25% of the score is inert for their profile.
#18 [MAJOR/agnostic] (agnostic) The Muse client hardcodes server-side 'Engineering'-only category filter, in a global (not per-project) config constant  <CONFIRMED effort=M>  @ config.py:169
    IMPACT: For ANY non-engineering user (health-IT VP, nurse, accountant, teacher) The Muse returns 0 results forever, regardless of keywords typed in the wizard or config.json — matches tonight's measured 'themuse 0' for dad. The source silently wastes a request/day per keyword and gives zero feedback that it
    REC: Make THEMUSE_CATEGORIES industry-driven: map cfg['industry']/DEFAULT_INDUSTRY to The Muse's real category taxonomy (Muse exposes Healthcare, Data Science, Business, etc.) via a small lookup table with 'Engineering'/'Science and Engineering' as the eng-industry default (byte-identical for Alex when industry is empty/eng-like, same pattern 
#19 [MAJOR/agnostic] (agnostic) DEFAULT_KEYWORDS (100% engineering titles) is the silent fallback in 4 code paths when a project has no configured keywords  <CONFIRMED effort=S>  @ config.py:140
    IMPACT: A non-technical health/finance/trade user who creates a project via workspace.create_project() (no config seeded — confirmed at workspace.py:236-237, no default content written) or runs daily_run.py/the MCP server/the browser extension before ever filling the wizard roles field gets silently searche
    REC: Make the fallback fail loud instead of eng-silent: when cfg has no 'keywords' and no industry set, daily_run/mcp_server/browser_receiver should skip the run (or surface a 'not configured yet' warning) rather than substituting Alex's DEFAULT_KEYWORDS. At minimum, gate DEFAULT_KEYWORDS behind `not cfg.get('industry')` so a project with an i
#20 [MAJOR/agnostic] (agnostic) DAILY_SOURCES hardcodes eng/tech-only remote boards with no per-project override  <UNVERIFIED effort=M>  @ config.py:209
    IMPACT: 5 of 9 daily sources (themuse, remoteok, remotive, jobicy, himalayas) are dead weight for any non-tech project — they consume the daily run's wall-clock and per-source rate limits for zero yield, and there's no way for a project to opt out or swap in health/general-purpose equivalents (jooble/career
    REC: Add an industry-aware source list: a per-project `sources` override in config.json, and default DAILY_SOURCES minus the confirmed-tech-only feeds when cfg['industry'] is set and non-eng-like (reuse match.facts.is_tech_industry). Surface jooble/careerjet setup in the wizard/Help when a non-tech industry is chosen, since they are the only w
#21 [MAJOR/agnostic] (agnostic) workspace.create_project seeds no keywords/industry, so non-wizard project creation silently inherits engineering defaults  <CONFIRMED effort=S>  @ workspace.py:236
    IMPACT: Multi-person support (person=project, per Session 23 handoff) means a second user's project can exist with an empty config.json if created via the People/Project button flow rather than the full wizard, immediately triggering the DEFAULT_KEYWORDS/DAILY_SOURCES engineering leak above for that person.
    REC: Either force the wizard to run for every newly created project (not just first-run), or have create_project accept/require industry+keywords and refuse a silent create with neither.
#22 [MAJOR/agnostic] (agnostic) rubric.py's non-exec seniority/years defaults are IC-engineering shaped, silently wrong for non-exec health/admin/trade roles too  <CONFIRMED effort=S>  @ match/rubric.py:18
    IMPACT: A non-exec, non-engineering seeker (the majority case for a nurse/accountant/tradesperson persona this dimension is asked to validate against) gets rubric defaults that don't reflect their field's normal seniority/years conventions, even though the exec branch got a dedicated fix in Session 23.
    REC: Extend the same industry-aware adaptation used in match/facts.py (is_tech_industry) to rubric.py: when cfg['industry'] is set and non-tech, drop the 'maintain' penalty (irrelevant/wrong signal outside engineering) and consider raising years_cap generically rather than only for detected exec intent.
#23 [MAJOR/ux] (ux) Scheduling the daily run has zero GUI surface — CLI/batch only, undiscoverable from the app  <UNVERIFIED effort=S>  @ scripts/setup_schedule.py:10
    IMPACT: The Guide repeatedly promises 'your Inbox also fills with fresh matches each day once daily updates are turned on' but never explains how to turn them on, and the mechanism (Windows Task Scheduler via a .bat in the install folder) is invisible from inside the running app. A non-technical user will l
    REC: Add a Tools/Help menu item 'Turn on daily updates' that shells out to scripts/setup_schedule.py (or an equivalent schtasks call) for the current project, with a status readback (task exists/next run time) shown in-app.
#24 [MAJOR/bug] (ux) Wizard's free-text roles field invites the exact narrow-phrase regression that caused tonight's near-zero reach  <CONFIRMED effort=S>  @ ui/setup_wizard.py:300
    IMPACT: Nothing in the UI stops a user from entering exec-title keywords, which silently produces a near-empty Inbox with no explanation tying it back to keyword phrasing — the search-tab 'No results' message (gui.py:2080-2082) only suggests 'broader keywords' generically, it doesn't diagnose phrase-vs-OR m
    REC: Add a short inline hint under the roles field: "Tip: use broad terms (e.g. 'health informatics', not 'Chief Medical Information Officer') — use Career level above for seniority." Optionally warn if the first post-setup search returns near-zero results.
#25 [MAJOR/measurement] (measurement) The correctly-designed job-level coverage estimator (loglinear capture-recapture + BLS JOLTS ground-truth gate) is fully built but has zero callers — completely dead code  <CONFIRMED effort=M>  @ coverage/benchmark.py:27
    IMPACT: The one estimator shaped to answer 'how many jobs in my field are actually reachable, and are we near saturation' never runs against real data. Only the weaker company-registry estimator (registry_coverage.py) is reachable at all, and only via CLI.
    REC: Wire run_benchmark into a small CLI (job_coverage.py) fed by the last N search runs' JobResults + soc_groups + optional BLS JOLTS key; print coverage/report.human_summary(). Works today even without a JOLTS key (cov_cr/cov_upper/c_hat still compute).
#26 [MAJOR/measurement] (measurement) The only automated 'independent' harvest source (Common Crawl) is merged straight into the registry by discover/funnel.py, destroying its usefulness as a recapture sample  <CONFIRMED effort=S>  @ discover/funnel.py:106
    IMPACT: Once a metro's cc_harvest results are absorbed into companies.json (the normal discovery flow), a later 'fresh' cc_harvest for the same hosts mostly re-finds slugs already in the registry — overlap m approaches n2, Chapman's n_hat collapses toward n1, and coverage_pct looks artificially close to 100
    REC: In funnel.py, snapshot the harvested board dict to a dated file (cache/coverage/harvest_snapshots/<industry>_<date>.json) BEFORE merging, and document that snapshot as the canonical --against input. Optionally add a staleness check in estimate_coverage_industry warning when list B's harvest date is close to the registry's last merge from 
#27 [MAJOR/measurement] (measurement) Per-source job counts from a real search run are printed to console and discarded — no persisted 'jobs per source per run' history exists  <CONFIRMED effort=S>  @ search/search_engine.py:126
    IMPACT: Directly blocks the requested 'jobs per source per run' dashboard component — no way to trend which sources are dead for a field/config over time without re-running and reading stdout by hand.
    REC: Have SearchEngine.search() return the per-source dict alongside deduped, and append one JSONL record per run to cache/coverage/search_runs.jsonl (industry, config hash, per-source counts, dedup total), reusing the append-only pattern already in coverage/registry_history.py:56 record().
#28 [MAJOR/measurement] (measurement) No pre-flight 'registry has N companies tagged for your field' check surfaces before a search — the 4-of-273 health-tag problem was only discoverable by reading companies.json by hand  <CONFIRMED effort=S>  @ scrape/company_registry.py:228
    IMPACT: A user configuring a new field (e.g. health informatics) gets no warning that the ATS-scraper path (273 companies for engineering) contributes ~0 companies for them; they only discover it after a live run returns near-zero jobs, exactly what happened tonight.
    REC: Add registry_stats(user_json=None) -> dict[industry_tag, count] to scrape/company_registry.py (trivial, iterate get_registry per tag) and surface it in the onboarding wizard / a GUI label at config-save time, with a visible low-count warning (e.g. <10 companies).
#29 [MINOR/reach] (reach) GUI and scheduled daily runs cap paginated sources at 1 page, undermining the broad-keyword recall strategy  <CONFIRMED effort=S>  @ gui.py:2040
    IMPACT: The measured-findings fix (broad field terms, e.g. 'nurse' -> 50 results on Adzuna) sits right at the page-1 ceiling — a broader keyword or larger metro will silently truncate before scoring/gating ever sees the tail, with no signal to the user that pagination stopped early.
    REC: Raise the GUI/daily default to 2-3 pages for keyword-parameterized sources (single-feed clients ignore page>1 anyway, so cost is bounded), or make it config-driven per project.
#30 [MINOR/reach] (reach) 'direct' fallback scraper (covers most large/enterprise registry employers) matches only on anchor link text, not descriptions  <CONFIRMED effort=M>  @ scrape/direct_scraper.py:138
    IMPACT: The exact employer class both personas most need (big health systems for Dad, big industrials for Alex) is routed to the weakest-matching scraper — a JS-routed careers portal with no literal keyword in its anchor text (common on modern SPA career sites) silently returns zero jobs every run, indi
    REC: Route scrape_direct's matching through `keyword_matches_deep` against nearby card text (not just anchor text); add a persistent-zero-result health-check flag (extending scrape/company_health.py's existing prune logic) for 'direct' companies so they surface as 'needs manual verification' rather than being silently trusted.
#31 [MINOR/bug] (scoring) Keywords double as both the search-API query AND the title-scoring query, baking narrow AND-phrases into scoring  <CONFIRMED effort=S>  @ search/cli.py:507
    IMPACT: Compounds finding #2: the same over-narrow, AND-conjoined phrases already responsible for near-zero search recall (measured_findings.md) are reused verbatim to score titles, so the scoring inherits the same brittleness — a title has to textually restate the exact keyword phrasing to get title credit
    REC: Separate the two concerns: keep broad OR-style field terms for search recall, and derive a distinct, softer title-scoring signal from the rubric's target_roles / seniority (e.g. reuse facts._detect_seniority + a title/keyword fuzzy-overlap rather than strict AND) so query-shape decisions for API recall don't dictate title-match strictness
#32 [MINOR/reach] (scoring) Exec-intent detection and allow_management gate collapse ALL management levels together — 'Senior Manager' gets identical treatment to VP/Director/CMIO  <CONFIRMED effort=M>  @ match/gate.py:37
    IMPACT: Even if finding #1 is fixed by wiring rubric/gate into the deterministic scorer, this gate itself would not lift target-level (VP/Director/CMIO) postings above generic manager-level noise — it only prevents them from being dropped outright. For a VP-seeking candidate, 'Senior Manager' and 'Chief Med
    REC: Split `_EXEC_RE` into tiers (e.g. C-suite/VP/Director vs plain manager) and thread a seniority-match score (not just a pass/fail gate) into both the deterministic scorer and the AI rubric, so a candidate targeting 'VP' explicitly outranks a 'Manager' title rather than merely not-dropping it.
#33 [MINOR/scoring] (scoring) Title-miss penalty double-counts a title miss on top of the weighted title contribution, making title phrasing dominate the 0-100 score  <CONFIRMED effort=S>  @ match/scorer.py:302
    IMPACT: Title phrasing alone can swing the visible score by up to ~70 of the 100-point budget (loss of ~35 scaled-weight + another flat -35), dwarfing skill/salary/location/recency combined for jobs whose description data is sparse (a common case for scraped/API-only postings with no description). This stru
    REC: Remove the redundant flat penalty (rely on the already-graded `t` term inside the weighted sum) or cap the miss penalty so title-related loss can't exceed a bounded fraction (e.g. 1.5x the title weight) of the total 100-point budget.
#34 [MINOR/ux] (ux) In-app help sends users to a README that is a single-line stub  <CONFIRMED effort=S>  @ README.md:1
    IMPACT: Any user who follows the app's own guidance to the README for key setup hits a dead end with zero instructions, undermining the otherwise well-written in-app Guide/Quick Start.
    REC: Either write a real Setup section in README.md (where .env lives, how to get each free key, that the exe uses SECRETS_DIR instead) or stop pointing to the README and route entirely through the in-app 'Connect job sources' panel above.
#35 [MINOR/agnostic] (ux) Guide tab's opening sentence hardcodes 'engineering jobs', contradicting the field-agnostic goal  <CONFIRMED effort=S>  @ ui/help.py:19
    IMPACT: A non-engineering user's first in-app impression is that the tool isn't built for them, right as the wizard is otherwise trying to be field-agnostic (industry/level fields added at ui/setup_wizard.py:316-330).
    REC: Change to field-neutral wording, e.g. "This app finds jobs that match what you're looking for...".
#36 [MINOR/ux] (ux) 'No sources configured' error message doesn't say which keys are missing or how to add them  <CONFIRMED effort=S>  @ gui.py:2078
    IMPACT: A stuck non-technical user sees a technical filename they don't recognize with no actionable next step, compounding the missing-key-UI gap above.
    REC: Once the 'Connect job sources' panel from finding 1 exists, change this message to open that panel directly (e.g. a clickable status link) instead of naming .env.
#37 [MINOR/bug] (measurement) loop_signal's plateau/dry verdicts have no guard against being computed from a correlated (non-independent) history round  <CONFIRMED effort=S>  @ coverage/registry_history.py:56
    IMPACT: A loop-until-dry acquisition script (per Session 23's handoff, `company_coverage.py --record --loop-signal`) could get a false DRY verdict driven by list-B correlation, silently capping registry growth for a field that's actually far from covered.
    REC: Add an independent:bool (or source_kind) field to the recorded JSON (registry_history.estimate_to_record's extra param already supports this) and have loop_signal ignore/flag non-independent rounds, or print a warning in --loop-signal output when list_b matches a recently-merged harvest.
#38 [ENHANCEMENT/measurement] (measurement) Reach-dashboard design: concrete, low-effort spec composing the existing (but disconnected) math into one 'are we near all the companies/jobs' view  <CONFIRMED effort=M>  @ coverage/registry_coverage.py:1
    IMPACT: Without a single composed view the user cannot answer 'are we near all the companies/jobs' at a glance; they'd have to separately read companies.json by hand, self-source a file for company_coverage.py, and read search console logs — exactly why the near-zero-reach problem for the dad persona shippe
    REC: Add a 'Reach' tab in gui.py with three pieces, all mostly-already-implemented plumbing: (1) Registry panel — registry_stats() per-industry counts (new) + last coverage estimate for the active industry from registry_history.load_history(); (2) Search panel — last N search_runs.jsonl entries (new) as a per-source table (source, jobs found, 
