# Tech-Debt Register — 2026-07-05 (S38 sweep)

39 findings from an 8-dimension read-only fleet (dead-code, duplication,
structure, error-handling, frontend, deps-packaging, docs-tests, us-first),
each dimension adversarially spot-verified. NOTE: the workflow's automated
verdict-join failed (title mismatch), so the orchestrator re-verified every
finding acted on tonight by hand. Two finder errors caught that way — recorded
here so future sessions don't repeat them:

- **#1 was WRONG about tests/test_application_cycle.py**: it tests LIVE
  tracker.db/service/analytics contracts (rounds, ICS, offers, notes), not the
  retired Flask app. Only the one flask-path test inside it was removed.
- **#2–#5 legacy/ files are NOT repo debt**: legacy/ is gitignored PERSONAL
  data (Dad's config, Alex's strategy docs). Left untouched.

## Triage (2026-07-05 night)

| Wave | Items | Status |
|---|---|---|
| D1 (orchestrator, inline) | #0 report :5001 link → web-Inbox deep link · #1/#15/#29 tracker/app.py family deleted · #6/#7/#30/#32 doc fixes · PORT_TRACKER/PORT_RESUME removed · #34 arbeitnow documented-as-is | DONE tonight |
| D2 (builder, frontend) | #19 per-tab lazy loading · #20 drop @dnd-kit/sortable (verify) · #21 useQueryGuard hook · #22 dead eslint-disable comments | dispatched |
| D3 (builder, backend) | #8 shared HTML-stripper (parity-proof per site) · #9 ISO-date dedup · #10 onboarding salary classification reuse · #11 db.py SQL helper · #17/#18 error guards · #31 test sleeps · #33 language-guard per-project country | dispatched |
| D4 (orchestrator, last) | #23 exe −30MB spec excludes + frozen smoke · #24 spec silent no-op log line · #26/#27 requirements hygiene | after builders |
| Deferred (registered) | #12 packaging/pyproject root-cause (L) · #13 tracker/db.py split (L) · #14 search→ui layering (M) · #16 tab_inbox split (L) · #25 gui.py lazy-tk (M) · #28 conftest tmp_db consolidation (M) · #21-followup other tabs | next debt session |
| Skipped (with reason) | #2–#5 legacy/ (personal data) · #35 jobsacuk trim (revival design deliberate, tests added S38) · #36 non-US metro tests (US-first) · #37/#38 (verifier: fine as-is) | — |

## Full findings

### 0. [dead-code] search/report_html.py links every generated report to the retired :5001 tracker
- **files:** search/report_html.py, search/templates/report.html, config.py
- **risk/effort:** low/S
- **evidence:** search/report_html.py:6,36 imports config.PORT_TRACKER (=5001, config.py:427) and passes it into report.html as tracker_port; report.html:281 renders `href="http://localhost:{{ tracker_port }}/add?..."`. generate_html_report() is called from two LIVE production paths: search/cli.py:607 and scrape/browser_receiver.py:474 (grep confirms only these + tests call it). tracker/app.py, the server that used to listen on :5001, carries its own docstring: 'DEPRECATED (retired S36, 2026-07-04) ... no longer part of the running product' (tracker/app.py:1-18).
- **why debt:** Every HTML report the app generates today (from a live CLI search or the browser-extension harvest flow) ships a per-job 'Add to tracker' link that 404s/connection-refuses, because nothing listens on :5001 anymore (S36 folded tracking into the :5002 receiver's /track endpoint). This isn't a comment or a historical artifact — it's a broken affordance shipped to real users in a currently-generated file.
- **proposed fix:** Point report.html's add-link at the :5002 receiver's /track (or equivalent) endpoint used by the rest of the S36 single-server story, or drop the per-job add-link from the HTML report entirely if the report is meant to be read-only now that the receiver/web UI is the tracking surface. Remove the PORT_TRACKER import/param once no template references it.

### 1. [dead-code] tracker/app.py (retired :5001 Flask tracker) kept alive only by 451 lines of its own tests
- **files:** tracker/app.py, tests/test_application_cycle.py, tests/test_w6a_csrf.py
- **risk/effort:** low/S
- **evidence:** tracker/app.py's own module docstring (lines 1-18) says: 'DEPRECATED (retired S36, 2026-07-04)... no longer part of the running product... Do NOT wire this into new code.' grep for `tracker\.app|tracker/app` across the repo finds exactly 2 non-comment/non-doc importers: tests/test_application_cycle.py (421 lines) and tests/test_w6a_csrf.py (30 lines) — both `from tracker.app import ...` style. Every other one of the 22 grep hits for the same pattern is a comment/docstring documenting the retirement (gui.py, popup.js, browser_receiver.py, run_servers.bat, webui/api/applications.py). No skip/xfail markers on either test file.
- **why debt:** docs/KNOWN_ISSUES.md itself lists 'deletion of the deprecated tracker/app.py (:5001 retired; file kept)' as a 'Pending Alex decision' — this is already flagged by the owner as awaiting a go/no-go, not a surprise. Reporting it here because it's the single largest concrete dead-code block in the repo (195 lines of app + 451 lines of tests = 646 lines exercising a server that cannot be reached from any live UI) and the two test files are pure cost (CI time, cognitive load) with zero product value once the go/no-go lands.
- **proposed fix:** Once Alex makes the pending call in KNOWN_ISSUES.md: delete tracker/app.py, tracker/templates/tracker.html, tests/test_application_cycle.py, tests/test_w6a_csrf.py, and PORT_TRACKER from config.py (after fixing the report_html.py finding above, which is the only other remaining consumer of that constant).

### 2. [dead-code] legacy/flatten_candidates.py hardcodes a dead Claude Code session temp path — cannot run
- **files:** legacy/flatten_candidates.py
- **risk/effort:** low/S
- **evidence:** legacy/flatten_candidates.py:4: `SRC = Path(r"C:\Users\alex_\AppData\Local\Temp\claude\E--ClaudeWork\4b13da16-4de2-49af-98b3-87fc4837b8ce\tasks\w20pmj1oa.output")` — a session-scoped Claude Code temp-file path from a prior (now-deleted) agent session. mtime/atime on the file and its sibling legacy/candidates.json (63,213 bytes) are both frozen at 2026-06-19; no other file in the repo references flatten_candidates.py (grep found zero importers).
- **why debt:** The script is unrunnable as committed (SRC will never exist again) and undiscoverable to a future session as the one-shot artifact it is — nothing marks it 'delete after running' the way its sibling legacy/_trim_inbox.py does. It's a config bug frozen into a checked-in file, not a general-purpose tool.
- **proposed fix:** Delete legacy/flatten_candidates.py (its output, legacy/candidates.json, has already been consumed by legacy/verify_and_add.py per the same June-19 timestamp cluster). If the candidate-flattening step is ever needed again, it should take SRC as a CLI arg, not a hardcoded session path.

### 3. [dead-code] legacy/_trim_inbox.py self-documents as one-time and reads a config path that no longer exists
- **files:** legacy/_trim_inbox.py
- **risk/effort:** low/S
- **evidence:** Docstring line 13: 'Delete this file after running.' Line 26-27 reads `(Path(__file__).parent / "user_config.json")` i.e. legacy/user_config.json, but only a root-level user_config.json exists (confirmed via find) — the read will always raise, always be swallowed by the bare `except Exception: pass` (lines 29-30), and CAP silently stays the hardcoded default of 15 regardless of the real per-company cap the user has configured.
- **why debt:** Explicitly labeled one-time-use code that was never deleted per its own instructions, and its narrow config-read is silently broken (wrong path) so if anyone ever did re-run it, it would use a stale/wrong CAP without any error surfacing — contradicts the project's own 'no silent drop' philosophy in miniature (a trim CAP that's silently wrong, not the visible one the user set in preferences).
- **proposed fix:** Delete legacy/_trim_inbox.py per its own instruction (the 959-row pre-Session-8 backlog it targeted is long processed, evidenced by the frozen June-10 mtime). If a general 'apply cap retroactively' tool is still wanted, it belongs in scripts/ reading the real active project's preferences, not a legacy path.

### 4. [dead-code] legacy/verify_and_add.py + legacy/candidates.json: one-shot company-registry seeding pipeline, dormant since June 19
- **files:** legacy/verify_and_add.py, legacy/candidates.json
- **risk/effort:** low/S
- **evidence:** Both files' mtimes are frozen at 2026-06-19 (3+ weeks with zero modification while the rest of the repo has commits through 2026-07-05). No other file imports verify_and_add.py (grep: zero non-legacy hits). Its docstring describes a specific one-shot task: probe candidate ATS slugs and append verified ones to companies.json.
- **why debt:** This is a finished, single-purpose data-migration script sitting in a folder named `legacy/` alongside truly dead files, making it easy to mistake for something still in the loop (or conversely to delete-by-association something still useful). Given the owner's 'blue-collar starter registry gap (#4)' open item in KNOWN_ISSUES — a future registry-buildout session may want exactly this verify-and-append pattern — this is borderline: not dead, but parked without a clear signal of reusability.
- **proposed fix:** Either (a) delete both files now that their one-shot job (June candidates batch) is done and companies.json has absorbed the results, or (b) if the pattern is meant to be reused for the pending blue-collar registry buildout, move verify_and_add.py out of legacy/ into scripts/ (alongside seed_companies.py, which does similar work) with a generic CAND path argument instead of a fixed candidates.json, and delete the now-stale candidates.json data file.

### 5. [dead-code] legacy/run_dad.bat + legacy/config_dad.json: superseded single-user launcher, doc says so
- **files:** legacy/run_dad.bat, legacy/config_dad.json
- **risk/effort:** low/S
- **evidence:** legacy/run_dad.bat:2 — 'LEGACY (superseded by the projects system; kept for reference)'. grep confirms config_dad.json is referenced from exactly one live path: scripts/migrate_to_projects.py:82, whose own docstring says it migrates config_dad.json into projects/dad-health-informatics and is guarded to abort 'if projects/projects.json already exists' (i.e. already run, per the current projects/ workspace model being live).
- **why debt:** Both files exist purely as reference/fallback for a migration that has already completed (projects/projects.json exists as the current model). Low value, but they're inert and harmless — grouping this with the other legacy/ findings for a single cleanup pass rather than fixing individually.
- **proposed fix:** Delete legacy/run_dad.bat and legacy/config_dad.json together with the migrate_to_projects.py one-time script once Alex confirms the dad-health-informatics project was migrated successfully (its projects/ entry should already show the row-parity the script's docstring guarantees).

### 6. [dead-code] gui.py module docstring references resume/app.py, which no longer exists
- **files:** gui.py
- **risk/effort:** low/S
- **evidence:** gui.py:10 docstring: 'Resume Generator — replaces resume/app.py (http://localhost:5000)'. `ls resume/` shows only __init__.py, docx_builder.py, experience_parser.py, generator.py, service.py, templates — no app.py. `git log --oneline -- resume/app.py` confirms it existed historically (commits from Session 6 era) but was removed at some point without the gui.py docstring being updated.
- **why debt:** Small, but it's a stale pointer in the most-read file's own header (1,491-line gui.py) misleading a future reader into thinking a resume/app.py module exists to cross-reference; PORT_RESUME (config.py:426) also has no remaining consumer once this is confirmed dead — worth checking in the same pass as the PORT_TRACKER cleanup above.
- **proposed fix:** Update the gui.py docstring to drop the resume/app.py reference (or state 'historically replaced resume/app.py, removed <date>' if historical context is wanted), and grep-confirm PORT_RESUME has no remaining importers before removing it from config.py.

### 7. [dead-code] webui/api/runs.py Phase-5 TODO is stale — the condition it was gating on already shipped
- **files:** webui/api/runs.py
- **risk/effort:** low/S
- **evidence:** runs.py:229-231: 'Phase 5 TODO: before the --web launcher ships, consider registration-time exclusion... rather than relying solely on this per-request gate.' webui/__main__.py:1-9,20 shows the --web launcher (and --desktop) already exist and the frozen exe already gets --web for free — i.e. the milestone this TODO was waiting for has already shipped.
- **why debt:** The TODO's own trigger condition ('before --web ships') has passed without the suggested registration-time exclusion being implemented — the current mitigation is a runtime double-check (TESTING flag AND pytest-in-sys.modules) rather than the not-registered-at-all design the TODO called for. Low urgency (the current gate is real, belt-and-suspenders) but the comment is now factually wrong about project state, which will mislead the next session into thinking this is still pre-ship prep work.
- **proposed fix:** Either implement the registration-time exclusion (only call _register_test_hook(bp) when a TESTING-only code path builds the app, never in the --web/--desktop app factory) and delete the TODO, or reword the comment to reflect that --web has shipped and this is now an accepted permanent runtime gate rather than a pre-ship TODO.

### 8. [duplication] HTML-tag-stripper (_TAG_RE) copy-pasted in 12+ scraper/client files despite an existing shared implementation
- **files:** search/single_feed_client.py:25,48-50, scrape/breezy_scraper.py:29,35, scrape/greenhouse_scraper.py:19,27-28, scrape/eightfold_scraper.py:43,63, scrape/jazzhr_scraper.py:38,48, scrape/personio_scraper.py:17,35, scrape/jsonld_scraper.py:19,25, scrape/paylocity_scraper.py:39,45, scrape/pinpoint_scraper.py:30,36, scrape/oracle_orc_scraper.py:48,211, scrape/phenom_scraper.py:44,202, scrape/recruitee_scraper.py:15,22, scrape/teamtailor_scraper.py:32,43, scrape/vincere_scraper.py:79,317-321, scrape/workable_scraper.py:15,22, search/careeronestop_client.py:55,185
- **risk/effort:** low/M
- **evidence:** `_TAG_RE = re.compile(r"<[^>]+>")` defined verbatim as a private module-level constant in 16 files (grep count). 12 of those additionally reimplement the exact same strip one-liner byte-for-byte: `re.sub(r"\s+", " ", _TAG_RE.sub(" ", html.unescape(raw))).strip()[:3000]` (verified identical across breezy/eightfold/jazzhr/personio/jsonld/paylocity/pinpoint/oracle_orc/phenom/recruitee/teamtailor/workable/vincere). Meanwhile `search/single_feed_client.py` already centralizes this exact pattern as `SingleFeedClient.strip_html()` with a comment stating it 'was duplicated as _TAG_RE in each' (search/single_feed_client.py:24) -- that consolidation only reached the search/ keyless-feed clients, not the scrape/ ATS scrapers, which re-invented the identical helper independently afterward.
- **why debt:** One regex + one truncation constant (3000) now has 13 independent copies to keep in sync. If the truncation length, unescape behavior, or tag-stripping regex ever needs a fix (e.g. a source starts emitting malformed markup that breaks the regex), it must be patched in 13 places by hand with no compiler/test enforcing consistency -- exactly the drift risk the SingleFeedClient docstring was written to eliminate, just not finished for the ATS-scraper family.
- **proposed fix:** Extract `_TAG_RE` and a `strip_html_to_text(raw, limit=3000)` helper into a shared module (e.g. `scrape/html_util.py` or reuse `search/single_feed_client.py`'s pattern via a stdlib-only shared util importable from both scrape/ and search/), then replace each per-file copy with an import. Pure refactor -- no behavior change, so it's safe under the parity lock (none of this touches match/scorer.py, ranker.py, or preferences gating).

### 9. [duplication] Heterogeneous ISO-date parser duplicated identically in 3 modules, one explicitly self-documented as a deliberate un-shared mirror
- **files:** search/freshness.py:48-59, search/search_engine.py:108-121, match/ghost.py:76-94
- **risk/effort:** low/S
- **evidence:** All three define a function with the identical body: strip + `.replace("Z", "+00:00")` then loop `for candidate in (s, s[:19], s[:10]): try: dt = datetime.fromisoformat(candidate); return dt if dt.tzinfo else dt.replace(tzinfo=timezone.utc); except ValueError: continue`. Only the not-found sentinel differs (`None` in freshness.py vs a module `_EPOCH` constant in search_engine.py and ghost.py). match/ghost.py:76-78 has an explicit comment: 'Mirror of search.search_engine._parse_created ... Inlined (not imported) to keep this module stdlib-only' -- so the third copy is a KNOWN, accepted duplication, but freshness.py's copy is undocumented and not acknowledged as a mirror of anything.
- **why debt:** Three copies of the same parsing tolerance logic (which formats/suffixes are accepted) mean a future fix to date-string tolerance (e.g. adding support for another upstream format) has to be manually ported 3x or silently drifts -- exactly the divergence pattern this project's own comment calls out as a tradeoff it accepted for one of the three, but seemingly forgot for the second.
- **proposed fix:** Extract the parse loop into a tiny stdlib-only shared helper (e.g. `_util/dateparse.py` with a `parse_flex_iso(value) -> datetime | None`) that both search_engine.py, freshness.py, and ghost.py call, each supplying its own not-found sentinel at the call site. Keeps ghost.py's stdlib-only constraint intact (the helper itself only needs `datetime`/`timezone`) while removing 3x drift risk. Non-behavioral, safe under parity lock.

### 10. [duplication] Web onboarding API reimplements the internal 'bare small number => hourly' classification that parse_salary_input already computes and discards
- **files:** webui/api/onboarding.py:150-173, ui/setup_wizard_core.py:84-112
- **risk/effort:** low/S
- **evidence:** `ui/setup_wizard_core.py:94,109` computes the numeric token and the `val < 1000` bare-small-number check internally inside `parse_salary_input`, but only returns the final annual int -- the 'was this read as hourly' classification is thrown away. `webui/api/onboarding.py:150-173`'s `/onboarding/salary-parse` route then re-derives that same classification from scratch with its own duplicate regex (`_re.search(r"(\d[\d,]*\.?\d*)\s*(k)?", text.strip().lower())`, onboarding.py:169) and its own `< 1000` bound check (onboarding.py:171) just to produce the `kind` field ('hourly'/'annual'/'none') for the UI.
- **why debt:** The bare-number/1000 threshold that decides 'annual vs hourly' now lives in two places that must move in lockstep; the second copy exists purely because `parse_salary_input` doesn't expose the classification it already derived. A future change to the threshold (e.g. tuning the $1000 cutoff) requires editing both functions or the API's reported `kind` silently disagrees with the actual annualized value it returns.
- **proposed fix:** Change `parse_salary_input` (or add a sibling `parse_salary_input_detailed`) in ui/setup_wizard_core.py to return `(annual, kind)` instead of just `annual`, and have webui/api/onboarding.py's route consume that tuple instead of re-deriving `kind` with its own regex. Low risk: internal to onboarding/wizard input parsing, not touched by the parity lock (scorer/ranker/preferences hard-gate).

### 11. [duplication] 'Fetch current application status by id' SQL query shape repeated 3x within tracker/db.py instead of a shared helper
- **files:** tracker/db.py:960, tracker/db.py:978, tracker/db.py:1005, tracker/db.py:1090
- **risk/effort:** low/S
- **evidence:** `"SELECT status FROM applications WHERE id=?", (job_id,)` (or the app_id-named equivalent) appears verbatim at db.py:978 (inside a note-add function), db.py:1005 (inside `entered_status_at`), and db.py:1090 (inside the interview-round insert path), plus the `SELECT *` variant at db.py:960. Each site re-executes the same single-column lookup inline rather than calling one small helper.
- **why debt:** Minor but real: any future change to how 'current status' is read (e.g. adding an archived-row guard, or a column rename) has to be hunted down and applied at 3-4 call sites in the same file with no single choke point, unlike the get_conn()-per-call pattern already flagged in KNOWN_ISSUES.md for a similar reason.
- **proposed fix:** Add a small private helper `_current_status(conn, job_id) -> str | None` in tracker/db.py and replace the three inline SELECTs with calls to it. Pure internal refactor, no schema or behavior change -- safe, low-blast-radius cleanup that can ride along with any nearby db.py touch.

### 12. [structure] No packaging config at all — root cause of 30+ sys.path.insert hacks and 23 flat root modules
- **files:** pyproject.toml (absent), setup.py (absent), pytest.ini (absent), config.py, daily_run.py, gui.py, mcp_server.py, claude_bridge.py, match/scorer.py, search/cli.py, tracker/db.py, tracker/app.py, tracker/service.py, resume/generator.py, resume/service.py, resume/experience_parser.py, scrape/browser_receiver.py, discover/inbox_harvest.py, discover/jobhive_seed.py, scripts/*.py (11 files), tests/conftest.py
- **risk/effort:** low/M
- **evidence:** grep for `sys.path.insert` returns 33 hits across 30 distinct .py files (repo root, tracker/, resume/, scrape/, discover/, search/, match/, scripts/, tests/). No pyproject.toml, setup.py, setup.cfg, or pytest.ini exists anywhere in the repo root (confirmed via `find . -maxdepth 1`). Repo root also holds 23 flat, unpackaged .py modules (applog.py, claude_bridge.py, config.py, daily_run.py, gui.py, industry_profile.py, insights.py, models.py, network.py, ranker.py, workspace.py, etc.) alongside proper packages (tracker/, match/, search/, ui/, webui/, scrape/, discover/, resume/, geo/, rerank/). Already flagged once in brain/review-2026-06.md:57 ('Testability debt — sys.path.insert everywhere') but never remediated. 8 test files under tests/ (e.g. tests/match/test_comp.py, test_ghost.py, test_semantic.py) duplicate the exact sys.path.insert conftest.py already does at collection time — pure copy-paste redundancy since conftest.py:19-21 already makes ROOT importable for every test in the tree.
- **why debt:** Every module resolves its own project root via `Path(__file__).resolve().parent[.parent]` and self-inserts into sys.path before importing sibling packages, instead of the project being pip-installable (`pip install -e .`) with real absolute imports. This is invisible today because everything is run via `py -3.12 script.py` from the repo root, but it means: (1) any script run from a different cwd or packaged differently breaks silently, (2) IDEs/type-checkers can't resolve imports without the same hack, (3) the PyInstaller spec (app.spec:26) has to replicate the same sys.path bootstrap a 4th time, (4) new contributors copy-paste the 3-line boilerplate into every new file (visible drift: some do `.parent`, others `.parent.parent`, correctness depends on file depth and is easy to get wrong one level off).
- **proposed fix:** Add a minimal pyproject.toml (or setup.cfg) declaring the project as an installable package (`pip install -e .`), delete the sys.path.insert boilerplate from all 30 files, and delete the 8 redundant per-test-file sys.path blocks (conftest.py already covers them). Purely mechanical — no behavior change, no scoring/ranker/preferences touch. Do NOT touch app.spec's bootstrap (PyInstaller frozen-mode needs its own path handling) without separately verifying the exe build.

### 13. [structure] tracker/db.py is a 1,897-line god-object mixing 6+ unrelated concerns behind one module
- **files:** tracker/db.py
- **risk/effort:** low/L
- **evidence:** wc -l tracker/db.py = 1897 (largest .py file in the repo, non-test). grep -c '^class |^def |^    def ' = 76 top-level definitions covering: SQLite connection lifecycle/pooling (get_conn, _new_conn, _alive, _retire, _sweep_dead_threads_locked, close_all_connections, checkpoint, release_for_restore — db.py:156-323), schema migration (init_db, _safe_add_column, _existing_columns — db.py:323-589), backup (rolling_backup, _is_dated_backup — db.py:611-663), job/application CRUD (add_job, update_job, delete_job, archive_job, get_job, get_all, get_counts — db.py:663-967), interview-round tracking (add_interview_round, list_interview_rounds, update/delete_interview_round — db.py:1064-1147), contacts (add_contact, list_contacts, contacts_for_company, delete_contact — db.py:1147-1196), and URL/dedup helpers (tracked_urls, dismiss_url, seen_urls, urls_not_seen, _url_host — db.py:1196+). Also independently flagged in docs/KNOWN_ISSUES.md:80-81 ('get_conn() opens a fresh SQLite connection per call — reuse redesign deferred, highest perf win, biggest blast radius').
- **why debt:** One module owns connection management, schema DDL, backup/restore, and five distinct entity CRUD surfaces (jobs, interviews, contacts, inbox rows, URL dedup). Any change to one concern (e.g. connection pooling) requires re-reading and re-testing a 1,900-line file that has nothing to do with the change. KNOWN_ISSUES.md already names the connection-per-call pattern as the single biggest deferred perf/risk item and explicitly cites 'biggest blast radius' as the reason it hasn't been touched — that blast radius is itself a symptom of everything living in one file with no separation between the connection layer and the CRUD layer.
- **proposed fix:** Split along the existing concern boundaries into tracker/db_conn.py (connection lifecycle + backup), tracker/db_jobs.py (job/application CRUD), tracker/db_interviews.py, tracker/db_contacts.py, keeping tracker/db.py as a thin re-export shim (`from .db_conn import *` etc.) so all 30+ existing call sites (gui.py, ui/tab_inbox.py, webui/api/inbox.py, search/search_job.py, etc.) keep working unchanged. Pure extraction, same pattern already proven safe in the S35b gui.py split. Do this BEFORE attempting the deferred get_conn() reuse redesign — right now that fix would have to touch a 1,900-line file end to end.

### 14. [structure] search/search_job.py (domain layer) imports from ui/ package — real layering inversion, obscured by a misleading directory name
- **files:** search/search_job.py:26, ui/tab_search_core.py, webui/api/board.py:27, webui/api/onboarding.py:34,182,199, webui/api/settings.py:28-29, webui/native_win.py:7 (comment), daily_run_core.py:36
- **risk/effort:** low/M
- **evidence:** search/search_job.py:26 does `from ui import tab_search_core as _core` — a search-domain module importing from the presentation package. Same pattern repeats: webui/api/board.py:27 imports `ui.kanban_core`, webui/api/onboarding.py imports `ui.setup_wizard_core` + `ui.ai_setup`, webui/api/settings.py imports `ui.settings` + `ui.source_keys_core`, daily_run_core.py:36 imports `ui.common._LineSink`. `grep -rln "^from ui\.|^import ui\." | grep -v ^./ui/ | grep -v tests | grep -v gui.py` returns 6 files outside the ui package proper (daily_run_core.py, search/search_job.py, webui/api/board.py, webui/api/onboarding.py, webui/api/settings.py, webui/native_win.py). ui/ contains 5 explicitly tk-free '_core.py' modules (help_core.py, kanban_core.py, setup_wizard_core.py, source_keys_core.py, tab_search_core.py) plus ui/common.py and ui/settings.py, each with a docstring stating 'nothing here imports tkinter' (e.g. ui/tab_search_core.py:9) specifically so webui/search/daily_run can reuse them without pulling in tkinter.
- **why debt:** Functionally these imports are safe today (the *_core.py modules are genuinely tkinter-free and unit-tested for it), but the package name 'ui' still reads as 'the desktop GUI' to any future contributor or static analyzer, so search/webui/daily_run depending on 'ui' looks like — and risks becoming — a real domain-on-presentation layering violation. The convention is undocumented outside scattered docstrings (no top-level ARCHITECTURE note says '_core.py files under ui/ are the shared logic layer'); a future edit to ui/theme.py or a new addition to ui/__init__.py side effects (module __init__ that imports tkinter transitively) would silently break webui/search/daily_run headless-import guarantees, and nothing currently enforces the boundary except by-convention naming plus scattered comments (webui/native_win.py:7 even explicitly routes AROUND ui.theme to avoid this exact risk).
- **proposed fix:** Move the 5 '*_core.py' files (plus ui/common.py, ui/settings.py, ui/source_keys_core.py) out of ui/ into a new shared package, e.g. core/ or app_core/, with ui/ re-importing from there for the tk tabs. Update the ~6 external call sites' imports accordingly. This makes the dependency direction match the code's actual (headless-safe) reality and lets a simple import-linter rule (e.g. 'ui/ must not be imported by webui/, search/, daily_run_core') catch future regressions. Pure rename/move, zero behavior change; do NOT touch ui/tab_inbox.py, ui/tab_search.py or other genuinely tk-bound tabs.

### 15. [structure] tracker/app.py (deprecated :5001 Flask server) still runs init_db() as an import-time side effect
- **files:** tracker/app.py:34-35
- **risk/effort:** low/S
- **evidence:** tracker/app.py:34-35: `app = Flask(__name__, template_folder="templates")` immediately followed by `init_db()` at module scope (not inside `if __name__ == "__main__"` or a function). The module's own docstring (tracker/app.py:15-16) already warns: 'WARNING: importing this module calls init_db() at import time (a project-touching side effect) — never import it from the web/webui process.' The file is explicitly marked DEPRECATED/retired (tracker/app.py:2, docs/KNOWN_ISSUES.md:62-63 'deletion of the deprecated tracker/app.py ... pending Alex').
- **why debt:** A dead-but-kept module that mutates the active project's SQLite DB (schema init/migration) merely by being imported is a live footgun: any future refactor, test collection change, or IDE auto-import that happens to `import tracker.app` (even transitively, e.g. via a wildcard or a tooling script that walks the tracker/ package) silently runs schema migration against whatever project is currently active — exactly the kind of surprise the module's own docstring is defensively warning against. It's already flagged as a pending-deletion candidate in KNOWN_ISSUES.md, so this finding is confirmation it's still live, not a new discovery.
- **proposed fix:** Since deletion is already 'pending Alex' per KNOWN_ISSUES.md, the low-risk interim fix (if keeping the file a while longer) is to move `init_db()` behind `if __name__ == "__main__":` so merely importing the module can never trigger it — that alone removes the footgun without deleting anything Alex hasn't signed off on. Full removal is Alex's call per the existing KNOWN_ISSUES note, not this review's to force.

### 16. [structure] ui/tab_inbox.py (1,367 lines) is the largest tk-bound tab and still mixes 4 concerns despite the S35b gui.py split
- **files:** ui/tab_inbox.py
- **risk/effort:** low/L
- **evidence:** wc -l ui/tab_inbox.py = 1367 — largest file under ui/ and 3rd largest .py in the repo after tracker/db.py and gui.py. Its import block (tab_inbox.py:1-30) pulls in: tk widget building (tkinter/ttk/messagebox/filedialog), scoring re-display (match.scorer.score_breakdown, extract_skill_terms), ghost/comp/ATS-hint annotation (match.ghost, match.comp, match.ats_hint), tracker CRUD (tracker.service, tracker.db.inbox_all/inbox_count/inbox_delete_urls), ranking (ranker as _ranker_mod), workspace/project switching, and inbox-health pruning (scrape.inbox_health.prune_inbox) — i.e. widget/view code, scoring-display logic, and data-access logic all in one 1,367-line file.
- **why debt:** This is the file most likely to need a change for ANY inbox-related feature (the single biggest tab), so its size directly taxes every future inbox change with re-reading 1,300+ lines that mix Tk view code with non-Tk business logic (score formatting, ghost/comp annotation orchestration). Unlike tab_search.py (which already has a tab_search_core.py sibling), tab_inbox.py has no equivalent extracted core module — the view and the non-Tk orchestration logic are still fused.
- **proposed fix:** Extract the non-Tk orchestration (row computation/annotation helpers like the ghost/comp/ATS-hint wiring, badge computation) into a tab_inbox_core.py sibling, following the exact precedent already established for tab_search.py/tab_search_core.py and the ui/*_core.py family. This would also let webui/api/inbox.py (which currently reimplements similar row-computation logic independently, e.g. its own _badges()/_computed() at webui/api/inbox.py:184-245) share the same core instead of maintaining parallel logic for parity by convention alone.

### 17. [error-handling] import_ai() has no exception handler around import_scores()/_read_text() — contradicts its own "never a 500" docstring
- **files:** webui/api/inbox.py, rerank/import_.py
- **risk/effort:** low/S
- **evidence:** webui/api/inbox.py:655 the `try:` block in import_ai() (route @inbox_bp.post("/inbox/import")) wraps the whole body but has ONLY a `finally:` at line 690 (tempfile cleanup) — no `except` clause. rerank/import_.py:34 `_read_text()` is `open(path,"r",encoding="utf-8-sig").read()` with zero exception handling, called unguarded from `import_scores()` (line 105), which import_ai() calls directly at inbox.py:682. The app-wide error hook (webui/__init__.py:111, `_api_http_error`) only registers for `HTTPException` — its own docstring at webui/__init__.py:31-43 says a view's uncaught error does NOT reach this handler. Every sibling route in the same API surface wraps its risky call in a local `except Exception as e:` returning a clean `{ok:false,error}` body (webui/api/queue.py lines 138, 160, 164, 193, 229, 268, 307) — import_ai is the outlier lacking that guard.
- **why debt:** The route's own docstring promises "A malformed file surfaces as result.errors (never a 500)" but a non-UTF-8 upload (legacy CP-1252/Excel export, or a corrupted paste saved to a temp file) raises UnicodeDecodeError inside _read_text(), propagates uncaught through import_scores() and import_ai(), and returns Flask's generic HTML 500 page instead of the documented JSON envelope — breaking the one contract the web UI otherwise guarantees everywhere else on this blueprint, for an easy-to-trigger real input (drag-and-drop a non-UTF-8 CSV/JSON export).
- **proposed fix:** Wrap the `import_scores(...)` call (webui/api/inbox.py:682) in its own `except Exception as e: return jsonify({"ok":False,"error":str(e)}),400` — the same pattern already used by every sibling route — or add a targeted `except (UnicodeDecodeError, OSError)` inside `_read_text()` in rerank/import_.py that reports a clean parse error into ImportResult.errors instead of raising.

### 18. [error-handling] ui/companies_dialogs.py _validate_worker() background thread has no exception guard — unlike every other Thread target in the codebase
- **files:** ui/companies_dialogs.py
- **risk/effort:** low/S
- **evidence:** ui/companies_dialogs.py:153-172 `_validate_worker(self, entries)` (started via `threading.Thread(target=self._validate_worker,...)` at line 150) calls `probe_board(e)` inside a `for` loop with no try/except anywhere in the function body. Contrast with every other GUI Thread target in the app, all of which wrap their body in try/except and marshal errors back via `.after()`: ui/tab_inbox.py:614-627 (`work()`), ui/tab_inbox.py:928-933, ui/tab_queue.py:347-354 (`worker()`), ui/tab_search.py:281-363 (`_worker`, try at 282/except at 362), ui/tab_resume.py:135-141 (`_worker`), ui/seed_area.py:146-167 (`_work`), ui/companies_dialogs.py:341-358 (`_run_orchestrator`, the SAME file's other worker, has a proper try/except at 354-357).
- **why debt:** If `probe_board()` raises for any entry (network timeout edge case, unexpected ATS response shape, etc.) the daemon thread dies silently: `_validate_done()` (line 174, re-enables the Validate/Detect buttons and resumes any pending Add) never fires, so the dialog is left with buttons stuck disabled (`_val_btn`/`_detect_btn` stay "disabled" from wherever `_detect()` left them) and no error message — a real bug (or transient network hiccup) becomes a stuck/dead-looking dialog with zero diagnostic trail, the exact class of leftover bug the S35b source-fetch fix was meant to close everywhere.
- **proposed fix:** Wrap the loop body in try/except per-iteration (report that one entry as "error" via `_set_status_cell` and continue, mirroring how a bad-count probe is already handled) plus a defensive except around the whole function so `_validate_done` always fires in a finally, matching `_run_orchestrator`'s pattern in the same file.

### 19. [frontend] 817KB single JS chunk — all 11 tabs eagerly bundled, zero lazy loading, though the registry makes per-route code-splitting mechanical
- **files:** webui/frontend/src/tabs/TabRoutes.tsx, webui/frontend/src/tabs/registry.ts, webui/frontend/src/App.tsx, webui/frontend/vite.config.ts
- **risk/effort:** low/M
- **evidence:** webui/static/assets/index-DhQkuPTt.js = 817,039 bytes (vite's default chunk-size warning threshold is 500KB — this build likely emits that warning). TabRoutes.tsx:4-16 statically imports all 11 tab components (InboxTab, TopPicksTab, SourcesTab, TrackerTab, BoardTab, InsightsTab, SearchTab, ApplyQueueTab, ResumeTab, GuideTab, DiscoverTab) and builds a READY_ELEMENTS map of already-constructed <Tab/> elements at module scope (lines 24-36) — so React.lazy can't even wrap the map values without restructuring to lazy component refs. Line-count evidence for weight: InboxTab.tsx 918 lines, TrackerTab.tsx 776, SearchTab.tsx 702, AiMenu.tsx 592, SeedAreaDialog.tsx 518, BoardTab.tsx 472 (+@dnd-kit), TopPicksTab.tsx 461, AddCompaniesDialog.tsx 448 — most users load Inbox only per session but pay for all of it. react-router-dom v7's <Routes>/<Route> (registry.ts uses plain component routing, not the v7 data router) is fully compatible with React.lazy()+Suspense.
- **why debt:** Every user pays the full 817KB (Board's @dnd-kit, Resume, Search's SearchRunConsole+SourceHealthStrip, Discover's experimental flow, etc.) on first paint of any single tab, even though only one tab is visible at a time and the registry.ts/TabRoutes.tsx structure already enumerates tabs as discrete, independently-routed units purpose-built for splitting.
- **proposed fix:** In TabRoutes.tsx, convert the READY_ELEMENTS entries to React.lazy(() => import("./inbox/InboxTab")) etc. (keyed the same way), wrap the <Routes> output in a single <Suspense fallback={<LoadingState />}> in App.tsx around <TabRoutes /> (states.tsx already exports LoadingState, reuse it — no new component needed). Keep DEFAULT_TAB (Inbox) as a normal eager import for first-paint speed if desired via manual chunking, or accept its one Suspense flash. ~10-15 line change, no behavior change to routing/gating logic.

### 20. [frontend] Genuinely unused dependency: @dnd-kit/sortable shipped but never imported
- **files:** webui/frontend/package.json, webui/frontend/src/tabs/board/BoardTab.tsx
- **risk/effort:** low/S
- **evidence:** package.json:17 lists "@dnd-kit/sortable": "^10.0.0". grep across all .tsx/.ts in src/ for `dnd-kit/sortable` returns zero import sites. BoardTab.tsx (the only drag-and-drop surface) imports only from @dnd-kit/core (DndContext, useDraggable, useDroppable, DragOverlay, etc., lines 3-15) and @dnd-kit/utilities (CSS, line 16) — it never uses useSortable/SortableContext/arrayMove from the sortable package. package-lock.json confirms sortable-10.0.0 is resolved/installed as a real, non-transient direct dependency.
- **why debt:** Dead dependency: adds install size and a supply-chain surface (one more package to audit/update) for zero functional benefit; nothing imports it.
- **proposed fix:** Remove "@dnd-kit/sortable" from package.json dependencies and run npm install to update package-lock.json. Zero behavior change since it's unreferenced.

### 21. [frontend] Repeated isLoading/isError query-guard boilerplate copy-pasted across 9 tabs — never abstracted into a hook, despite states.tsx already centralizing the render output
- **files:** webui/frontend/src/tabs/board/BoardTab.tsx, webui/frontend/src/tabs/guide/GuideTab.tsx, webui/frontend/src/tabs/insights/InsightsTab.tsx, webui/frontend/src/tabs/sources/SourcesTab.tsx, webui/frontend/src/tabs/toppicks/TopPicksTab.tsx, webui/frontend/src/tabs/tracker/TrackerTab.tsx, webui/frontend/src/tabs/inbox/InboxTab.tsx, webui/frontend/src/tabs/queue/ApplyQueueTab.tsx, webui/frontend/src/tabs/inbox/InboxDetail.tsx, webui/frontend/src/components/states.tsx
- **risk/effort:** low/S
- **evidence:** Verbatim-shaped block repeated 9 times, e.g. BoardTab.tsx:114-127 and InsightsTab.tsx:37-50: `if (query.isLoading) return <LoadingState />; if (query.isError) { return (<ErrorState title="Couldn't load your X" message={query.error instanceof ApiError ? query.error.message : "The X service didn't respond."} onRetry={() => query.refetch()} />); }`. Same `err/error instanceof ApiError ? .message : "<hardcoded fallback>"` idiom appears independently in each file. states.tsx (the shared render-output components, cited correctly by repo CLAUDE.md conventions) stops at exporting <LoadingState>/<ErrorState>/<EmptyState> — it never exports the *guard logic* that decides which to render from a TanStack Query result object, so each tab reinvents that decision tree.
- **why debt:** 9x duplication of the same control-flow + string-fallback idiom; any future change to the guard behavior (e.g. adding a stale-data banner, retry backoff, or distinguishing network vs. 4xx errors) requires touching 9 files identically instead of one hook. It's exactly the 'shared fetch-render-empty-error scaffolding' states.tsx was built to centralize, just one layer short.
- **proposed fix:** Add a small hook, e.g. `useQueryGuard(query, { title, fallback }): ReactNode | null` in components/states.tsx (or a new lib/query-guard.tsx) that returns the <LoadingState>/<ErrorState> element or null when the query is ready, called as `const guard = useQueryGuard(query, {title: "Couldn't load your board", fallback: "The board service didn't respond."}); if (guard) return guard;` at the top of each tab. Mechanical, low-risk, no change to what's rendered.

### 22. [frontend] 6 eslint-disable-next-line comments are dead weight — no ESLint is installed, configured, or runnable in this project
- **files:** webui/frontend/package.json, webui/frontend/src/components/ai-setup-dialog.tsx, webui/frontend/src/lib/useJobConsole.ts, webui/frontend/src/lib/useUrlSyncedState.ts, webui/frontend/src/tabs/queue/BatchDialog.tsx
- **risk/effort:** low/S
- **evidence:** package.json has no "lint" script and no eslint/eslint-plugin-* in devDependencies; `find` for .eslintrc*/eslint.config* in webui/frontend/ returns nothing; node_modules/eslint does not exist (no node_modules/.bin/eslint). Yet 6 lines across 4 files carry `// eslint-disable-next-line react-hooks/exhaustive-deps` (useUrlSyncedState.ts:64,78,91; useJobConsole.ts:129; ai-setup-dialog.tsx:82; BatchDialog.tsx:100).
- **why debt:** These comments are inert — no linter reads them — so the react-hooks/exhaustive-deps warnings they suppress have never actually been re-verified since ESLint was (presumably) removed from the toolchain; the comments now just document an intent with zero enforcement. Not a functional bug (useUrlSyncedState.ts's cases are correctly reasoned via refs, per its own comments), but it's a misleading trail for the next contributor who assumes lint is running.
- **proposed fix:** Either (a) reinstall eslint + eslint-plugin-react-hooks + a flat config and add a `lint` script so these directives do something again, or (b) if lint tooling was deliberately dropped, strip the now-meaningless `eslint-disable` comments (keep the surrounding explanatory prose, which is legitimate) so the codebase doesn't imply a check that doesn't exist. Either is a small, contained change.

### 23. [deps-packaging] ~30MB (31% of the frozen exe) is dead weight for an off-by-default, non-functional semantic-ranking feature
- **files:** match/semantic.py, app.spec, config.py:497
- **risk/effort:** low/S
- **evidence:** dist/JobProgram/_internal (96.4MB total, 1,347 files) bundles numpy (3.8M+numpy.libs 13M), tokenizers (4.4M), hf_xet (5.9M), safetensors (472K), pydantic_core (3.1M) = 29.9MB. These are transitive deps of `model2vec` (match/semantic.py:83 `from model2vec import StaticModel`), pulled in because PyInstaller's static AST scan of `collect_submodules('match')` in app.spec sees the literal `import numpy as np` at match/semantic.py:105,128 and treats it as a hard reference, bundling numpy's own dep chain. But `model2vec` itself is NOT bundled (no dist/JobProgram/_internal/model2vec dir exists) and no bundled model dir exists at data_static/models/potion-base-8M. config.py:497 `SEMANTIC_RANKING = os.getenv(...) not in ("","0","false","False","no")` defaults OFF. Net effect: the exe permanently carries ~30MB of numpy/tokenizers/hf_xet/safetensors that can never do anything useful, because the one package that would use them (model2vec) isn't in the bundle, and the feature is off by default anyway.
- **why debt:** Silent, unbounded bloat: every future app.spec rebuild that adds any first-party module importing numpy-family packages (even inside a function, even for an optional feature) drags the whole dep tree into hard bundling via collect_submodules' static scan. Nobody chose to ship numpy in a job-search GUI exe; it's an emergent side effect of the lazy-import pattern colliding with PyInstaller's static analysis, and it currently buys nothing (model2vec absent, feature off).
- **proposed fix:** Either (a) add `model2vec` + a bundled potion-base-8M model dir to app.spec's datas/hiddenimports so the feature actually works in the exe (turning the 30MB from waste into a real feature), or (b) add `excludes=['numpy','tokenizers','hf_xet','safetensors','model2vec','huggingface_hub']` to the `Analysis(...)` call in app.spec so the gated try/except in semantic.py degrades to `available()==False` in the exe exactly as it already does when model2vec is missing at runtime -- consistent with the documented 'fully optional + gated, no-op if absent' design. (b) is the low-risk pick since it matches current shipped behavior (semantic ranking already doesn't work in the exe) while reclaiming ~30MB.

### 24. [deps-packaging] Task's assumed scrapling/playwright/patchright/browserforge/curl_cffi stealth-fetch stack is NOT in this reference build at all -- app.spec's best-effort collection silently no-ops
- **files:** app.spec:113-124, requirements.txt:16-24
- **risk/effort:** low/M
- **evidence:** app.spec lines 119-124 wrap `collect_submodules(_opt)` / `collect_data_files(_opt)` for scrapling/playwright/patchright/browserforge in a bare `try/except Exception: pass`. `py -3.12 -c "import importlib.metadata; importlib.metadata.distribution('scrapling')"` raises `No package metadata was found for scrapling` in the current dev env (matches requirements.txt:20-21's own comment: 'not importable in this dev environment as of 2026-07-01'). Correspondingly, dist/JobProgram/_internal has zero scrapling/playwright/patchright/curl_cffi/browserforge/msgspec/orjson directories -- confirmed absent. So the actual measured 96.4MB / 1,347-file bundle already ships WITHOUT the stealth stack; the ~140MB figure some builders may be assuming reflects a build machine where scrapling *is* pip-installed.
- **why debt:** The bare `except Exception: pass` means a broken/partial scrapling install on a future build box fails silently -- no error, no log line -- and the resulting exe just quietly lacks stealth-fetch capability with no diagnostic trail. Combined with requirements.txt pinning `scrapling==0.4.9` as an unconditional line (not a documented-optional extras group), a fresh `pip install -r requirements.txt` on a clean machine WILL pull the full playwright/patchright/curl_cffi/browserforge chain and this becomes a large, real bundle -- the download-on-enable question from the task is live for that path, just not reproduced in the current dev env's build.
- **proposed fix:** Split scrapling+its transitive stack into a separate `requirements-stealth.txt` (mirroring the existing requirements-mcp.txt pattern already used to keep the MCP channel out of the lean GUI build), install it only via the in-app 'Enable stealth fetching' flow (pip install at runtime, matching the existing 'Chromium downloaded on demand' design already documented in the app.spec comment), and replace the bare `except Exception: pass` in app.spec:119-124 with a one-line print/log so a partial/missing install is visible in the build log rather than silent.

### 25. [deps-packaging] gui.py imports tkinter + ttkbootstrap (+ PIL transitively) at module level, so `--web`-only and `--daily` headless runs still pay that import cost even though they never create a Tk window
- **files:** gui.py:25-26, gui.py:1442-1478
- **risk/effort:** low/S
- **evidence:** gui.py:25-26 `import tkinter as tk` / `from tkinter import ttk, messagebox, simpledialog, filedialog` are unconditional top-of-file imports (ttkbootstrap import follows similarly further down). `main()` (gui.py:1442) only branches on `--web`/`--desktop`/`--daily` AFTER the module has already fully imported, per gui.py:1458 `if "--web" in sys.argv[1:]...: from webui.__main__ import main as _web_main; return _web_main(...)`. Because Python must execute the whole module body before `main()` is even called, every invocation of the single JobProgram.exe entry point -- including pure headless `--daily` scheduled-task runs and `--web` server-only runs -- imports tkinter, ttkbootstrap, and PIL (9.5MB in dist/_internal) into the process even though those code paths never touch a widget.
- **why debt:** This is exactly the 'ttkbootstrap+PIL pulled into the WEB-only exe path' pattern called out in scope. It doesn't add bundle bytes (tkinter is stdlib and ttkbootstrap/PIL are already bundled for the tk GUI mode, which the task retains), but it adds real import-time cost and process memory to the --daily scheduled-task path (runs unattended, nightly, per docs/handoffs) and to --web server mode, for zero benefit.
- **proposed fix:** Move the `import tkinter` / `from tkinter import ...` / ttkbootstrap imports from module level into the `App` class's `__init__` or a small `_run_tk_gui()` function that's only called in the final fallback branch of main() (gui.py:1475-1476), so `--web`/`--daily` short-circuit before ever importing tkinter/ttkbootstrap. Low-risk, mechanical refactor; App() is already the only unconditional call site left after the --web/--daily/--desktop early returns.

### 26. [deps-packaging] Multiple prod modules hard-import third-party packages with no requirements.txt entry and no gating (certifi; also model2vec/statsmodels/pywebview are gated but wholly undocumented outside inline comments)
- **files:** search/reap_client.py:71, coverage/estimators.py:6,115, match/semantic.py:83, webui/__main__.py:116, CLAUDE.md:13
- **risk/effort:** low/S
- **evidence:** AST scan of every first-party .py file (excluding tests/dist/build) surfaces third-party top-level imports not declared in requirements.txt or requirements-mcp.txt: `certifi` (search/reap_client.py:71, unconditional `import certifi` inside `_reap_ca_bundle()`, no try/except), `model2vec`+`numpy` (match/semantic.py, gated), `statsmodels` (coverage/estimators.py:6 `try: import statsmodels.api as _sm / except ImportError: _HAVE_SM = False`, gated), `webview`/pywebview (webui/__main__.py:116, gated, and separately documented in CLAUDE.md:13's 'Deps beyond .env' list -- but model2vec/statsmodels are NOT in that list). certifi currently 'works' only because it rides in transitively via `requests`' own certifi dependency, not because it's declared for JobScout's own direct use.
- **why debt:** certifi is the concrete risk: it's a hard, non-gated import for the REAP source's TLS-chain workaround, but nothing in requirements.txt pins or declares it -- if `requests` ever stops vendoring/requiring certifi (or a user builds a stripped venv with `--no-deps`), `search/reap_client.py` breaks with an unhandled ImportError the first time a REAP-source job search runs, with no gating comparable to the model2vec/statsmodels pattern used elsewhere in the same codebase. Separately, model2vec and statsmodels being real (if optional) runtime dependencies that exist in NEITHER requirements.txt NOR CLAUDE.md's 'Deps beyond .env' list means a future session standing up a fresh dev box will hit two more undocumented ModuleNotFoundErrors beyond the five already listed there.
- **proposed fix:** Add `certifi` as an explicit requirements.txt line (cheap, it's already transitively present via requests so this is a zero-behavior-change pin). Add `model2vec`, `statsmodels` to CLAUDE.md's 'Deps beyond .env' list (or a new 'Optional (gated) deps' sub-list) alongside the existing five, so `docs/KNOWN_ISSUES.md`/onboarding notes stay honest about what a full-featured dev environment actually needs.

### 27. [deps-packaging] requirements.txt's pyinstaller pin (6.11.1) is 10 minor versions behind the version actually used to produce the shipped exe (6.21.0)
- **files:** requirements.txt:9
- **risk/effort:** low/S
- **evidence:** requirements.txt:9 pins `pyinstaller==6.11.1`. `py -3.12 -m PyInstaller --version` in this environment reports `6.21.0`. The dist/JobProgram bundle being audited was necessarily built with whatever PyInstaller version is actually installed (6.21.0), not the pinned 6.11.1.
- **why debt:** The whole point of a pin (per the scrapling comment 2 lines below it explaining why scrapling got pinned instead of left floating) is reproducibility -- so a build box installing from requirements.txt fresh gets a different PyInstaller than the one that actually produced the artifact under review, which can silently change bundle contents (PyInstaller's own hook/collect behavior has changed across those 10 versions) between what's documented and what's shipped.
- **proposed fix:** Bump the pin to match the installed/verified version (`pyinstaller==6.21.0`) or re-verify against whatever version the release process actually uses, same treatment already given to scrapling's pin.

### 28. [docs-tests] 28 test files hand-roll their own near-identical `tmp_db` fixture instead of a shared conftest fixture
- **files:** tests/conftest.py, tests/test_application_cycle.py, tests/test_byoai_auto_rank.py, tests/test_byoai_undo_parity.py, tests/test_c1_job_key_dedup.py, tests/test_compact_service.py, tests/test_contacts.py, tests/test_db_conn_reuse.py, tests/test_db_pragmas.py, tests/test_followups_due.py, tests/test_freshness_wiring.py, tests/test_inbox_fts.py, tests/test_mcp_cycle_tools.py, tests/test_review_fixes_s25.py, tests/test_review_fleet_fixes.py, tests/test_runs_table.py, tests/test_s32_lifecycle_fixes.py, tests/test_status_history.py, tests/test_top_picks.py, tests/test_tracker_archive.py, tests/test_tracker_db.py, tests/test_tracker_service.py, tests/test_url_index_antijoin.py, tests/test_w3a_data.py, tests/test_w3b_undo.py, tests/test_w4_toppicks.py, tests/rerank/test_db_migration_v3.py, tests/rerank/test_roundtrip.py, tests/rerank/test_service_rerank.py
- **risk/effort:** low/M
- **evidence:** grep '^def tmp_db(' across tests/*.py + tests/rerank/*.py hits exactly 28 files, each defining the identical 3-line body `monkeypatch.setattr(db, "DB_PATH", tmp_path / "tracker.db"); return db.DB_PATH` (or the same 4-line variant that also calls `db.init_db()`). tests/conftest.py (the root conftest) exists and already carries other shared fixtures (network fencing, per-run module reset) but has NO tmp_db fixture at all — the webui subtree DOES centralize its equivalent in tests/webui/conftest.py:26-30, so the pattern for doing this correctly already exists in-repo, it just wasn't applied to the top-level tests/ tree.
- **why debt:** Any future change to how tracker.db is test-isolated (e.g. switching from monkeypatched DB_PATH to a fixture-injected connection, or adding WAL-mode setup) requires editing 28 files instead of 1. It's also the kind of copy-paste that already silently forked into two slightly different variants (with/without init_db()), so a maintainer copying the wrong one gets a confusing collection-time or first-test failure with no schema.
- **proposed fix:** Add two fixtures to tests/conftest.py: `tmp_db` (monkeypatch DB_PATH only, matching the current majority shape) and `tmp_db_initialized` (same + init_db()) — or a single fixture parametrized by an `init=True` marker/param. Delete the 28 local redefinitions; pytest fixture resolution picks up the conftest version automatically with zero call-site changes since the fixture name is unchanged.

### 29. [docs-tests] test_w6a_csrf.py tests CSRF guards on tracker/app.py, the retired :5001 Flask surface pending deletion
- **files:** tests/test_w6a_csrf.py, tracker/app.py
- **risk/effort:** low/S
- **evidence:** tests/test_w6a_csrf.py imports `tracker.app as tapp` and its 4 tests (test_foreign_origin_post_is_forbidden, test_loopback_origin_post_not_forbidden, test_extension_origin_api_add_not_forbidden, test_no_origin_not_forbidden) all POST against tapp.app.test_client() routes (/delete/*, /api/add) with an Origin header literally set to 'http://localhost:5001'. docs/KNOWN_ISSUES.md states ':5001 retired' and lists 'deletion of the deprecated tracker/app.py (:5001 retired; file kept)' as a 'Pending Alex decision' — the file is intentionally kept for now, but this test file is pure CSRF-surface coverage for code the project has already decided is dead weight.
- **why debt:** This is coverage that only has value as long as tracker/app.py stays reachable in production; today it isn't (the web UI + tk GUI are the shipped surfaces). It inflates the suite with tests whose failure would not indicate a real user-facing regression, and it will need deleting the same day Alex approves removing tracker/app.py — better to flag now than rediscover it during that cleanup.
- **proposed fix:** No action needed until Alex approves tracker/app.py deletion (KNOWN_ISSUES already tracks that decision) — but when that happens, delete tests/test_w6a_csrf.py in the same commit as tracker/app.py, and also drop the one Flask-path assertion inside test_application_cycle.py (test_flask_update_path_arms_follow_up, lines 70-78, which does `from tracker import app as flask_app`) since it duplicates coverage already provided by the db.update_job / service.set_status tests directly above it in the same file.

### 30. [docs-tests] _index.md Core Documents table still describes gui.py as a "5-tab GUI", app has grown to 8 tabs
- **files:** _index.md
- **risk/effort:** low/S
- **evidence:** _index.md:55 reads: '`gui.py` / `daily_run.py` | 5-tab GUI + scheduled daily-run entry points.' gui.py:1096-1104 instantiates 8 tabs on the notebook: InboxTab, TopPicksTab, SearchTab, ApplyQueueTab, TrackerTab, KanbanTab, ResumeTab, uihelp.GuideTab. The '5-tab' figure matches the session-14-era description (row 49 of the same table: 'UI/UX pass... first-run Setup wizard' for handoff_20260622_session14) but was never updated as Top Picks, Board (Kanban), and Guide were added in later sessions.
- **why debt:** This is the project's own orientation index contradicting the current code on a directly-checkable fact (tab count) — a new session or contributor reading _index.md's Core Documents table for a quick orientation gets a wrong mental model of the GUI's scope.
- **proposed fix:** Update _index.md:55 to '8-tab GUI (Inbox, Top Picks, Search, Apply Queue, Tracker, Board, Resume, Guide) + scheduled daily-run entry points.' — one-line fix, no other references to the stale count found elsewhere.

### 31. [docs-tests] webui test files use real sub-100ms sleeps for concurrency/ordering assertions instead of deterministic clocks
- **files:** tests/webui/test_companies.py, tests/webui/test_guide.py, tests/webui/test_jobs.py, tests/webui/test_runs_daily.py, tests/webui/test_runs_daily_knobs.py, tests/webui/test_search.py
- **risk/effort:** low/S
- **evidence:** grep 'time.sleep' across tests/ hits 12 real (non-monkeypatched) sleeps inside tests/webui/*.py: test_companies.py:35 (0.01s), test_guide.py:233 (0.05s), test_jobs.py:21/120/171/205/217/240 (0.01s x6), test_runs_daily.py:69/199/241 (0.01s x3), test_runs_daily_knobs.py:55 (0.01s), test_search.py:61/244 (0.01s x2) — 13 real sleeps totaling ~150ms of pure wall-clock waiting per full run, versus tests/test_http_util.py which monkeypatches time.sleep entirely (the correct pattern already used elsewhere in the same suite).
- **why debt:** Individually tiny (0.01-0.05s), but it's the textbook 'test suite death by a thousand small sleeps' pattern, and worse, timing-based sleeps in concurrency/ordering tests are inherently flake-prone under CI load (a sleep(0.01) chosen to separate two timestamps can collide on a slow/loaded runner). The suite already has the fix pattern in-repo (monkeypatch time.sleep) but it wasn't applied to these 6 webui files.
- **proposed fix:** Where the sleep exists purely to force two operations onto different timestamps/orderings (the common case for job-list and run-history tests), replace with either monkeypatched time/datetime or an explicit incrementing counter/fake clock fixture instead of a real sleep. Total suite-time savings are small (~150ms) but the bigger win is removing timing-flake risk in CI.

### 32. [docs-tests] docs/KNOWN_ISSUES.md still lists 3 free-key sources as 'unclaimed' and Discover-tab removal as open, but neither has a re-check date or owner
- **files:** docs/KNOWN_ISSUES.md
- **risk/effort:** low/S
- **evidence:** KNOWN_ISSUES.md:74-76 ('Free keys unclaimed... the single biggest untapped jobs-found lever (~15 min of signups, needs Alex)') and :66-71 (Discover tab 'EXPERIMENTAL, may be removed' with an exact deletion recipe already written) are both carried forward across S35/S36/S36c/S37 session entries in _index.md without the KNOWN_ISSUES entry itself being dated/updated — it's not wrong, but it's a living doc accumulating open items whose staleness can't be told apart from currency (no 'last verified' timestamp per entry, unlike the dated session entries in _index.md).
- **why debt:** Low severity — flagging only because the task calls for docs that could mislead; this one doesn't contradict the code today, it's just an undated backlog note mixed into a 'living document' of accepted trade-offs. Not urgent, borderline exclude, included only because it is the one place a genuinely stale claim could hide undetected next session.
- **proposed fix:** Optional: add a one-line 'last verified: <date>' to the Known issues (accepted for now) section header so future sessions can tell at a glance whether the open items are still open without re-deriving it from grep.

### 33. [us-first] Language guard reads a process-global env var, not the per-project resolved country — the guard's own docstring claim ("non-US Adzuna country") doesn't match what it checks
- **files:** config.py, daily_run.py, match/language.py
- **risk/effort:** low/S
- **evidence:** config.py:624-631 language_guard_active() checks only module-level ADZUNA_COUNTRY (config.py:190, set once from os.getenv at import time) or the LANGUAGE_GUARD env flag. daily_run.py:416 calls `_cfg.language_guard_active()` with no argument. Meanwhile every other country-routing path (adzuna/jooble/careerjet, source_registry.py:66-209) derives the ACTIVE PROJECT's country per-call via `_cfg.adzuna_country_for(location)` (search/cli.py build_clients, L99) using the project's own location string — a project-scoped value that never reaches language_guard_active(). tests/test_b2_source_keys.py:360-376 only exercises the guard by monkeypatching config.LANGUAGE_GUARD / config.ADZUNA_COUNTRY directly; no test drives it through an actual per-project daily_run with a non-US project while the process/env default is 'us'.
- **why debt:** In a single long-lived process serving multiple projects (Zaggregate's normal multi-person/multi-project model, per repo docs), this is the one country-sensitive mechanism that is NOT project-scoped like its siblings. A UK project's non-English postings will silently NOT get the guard (scored confidently by an English keyword matcher = wrong-for-the-wrong-reason results) unless the operator also exported ADZUNA_COUNTRY=gb or LANGUAGE_GUARD=1 as an env var for that whole process — an easy-to-miss manual step with no code-level enforcement, and the inverse (env left set to a stale non-US value) would incorrectly arm the guard for a US project too. It is a correctness gap specific to the international surface, not a style nit: the fix path (thread country through, same pattern already used for build_clients) is a small, safe, mechanical change with no scoring-formula impact (match/scorer.py untouched).
- **proposed fix:** Thread the resolved per-project country into language_guard_active(country) the same way build_clients threads it into BuildContext, i.e. `_cfg.language_guard_active(_cfg.adzuna_country_for(location))` at daily_run.py:416, defaulting the param to the current global-only behavior for back-compat. Add one test that arms the guard via a non-US PROJECT (not just env) while ADZUNA_COUNTRY stays 'us'.

### 34. [us-first] arbeitnow runs full network fetch + parse for every US project — the one international-flavored source with zero country self-gate
- **files:** search/source_registry.py, search/arbeitnow_client.py, search/cli.py
- **risk/effort:** low/S
- **evidence:** source_registry.py:179-181 `_arbeitnow` builder has no country check (contrast with `_usajobs`/`_careeronestop` at L99-134 which explicitly skip + `ctx.note_country_skip(...)` for non-'us'). `arbeitnow` is listed in `ALL_SOURCES` (search/cli.py:20-25) and absent from `OPT_IN_SOURCES` (cli.py:33), so it is on-by-default for every project including 100% US ones. arbeitnow_client.py fetches the ENTIRE feed once per cache cycle (`self.session.get(ARBEITNOW_URL)`, a German/EU-centric board per its name/docstring) then keyword-filters client-side (arbeitnow_client.py:15-53) — real network + parse cost paid by US runs for a source whose results will mostly fail a US user's downstream location match. It IS tagged in `TECH_SKEWED_SOURCES` (search/keyword_strategy.py:227) so it gets some relevance weighting, but that's a scoring nuance, not a gate.
- **why debt:** This is the mirror-image gap of the already-fixed '#12 US-only sources for non-US' item in KNOWN_ISSUES.md (fixed: usajobs/careeronestop now skip for non-US) — but nobody closed the reverse direction for EU-flavored free sources. It's the single clearest "international work executes on the US path" finding in the whole registry; every other international-only source (adzuna/jooble/careerjet country hosts, jobsacuk) is either self-gated or a no-op string-swap for 'us'.
- **proposed fix:** Leave results eligible (inclusion-over-precision philosophy — do NOT drop the source or filter it for US users), but consider a cheap early-exit only if profiling shows real cost (cache makes this a per-cycle, not per-request, fetch, so likely low actual impact — verify before spending effort). If low-impact, downgrade to 'leave as-is, documented' rather than code changes.

### 35. [us-first] jobs.ac.uk retired module kept fully intact (193 LOC) with dead opt-in machinery, zero test coverage — biggest inert-but-present surface
- **files:** search/jobsacuk_client.py, search/source_registry.py
- **risk/effort:** low/S
- **evidence:** search/jobsacuk_client.py = 193 LOC total; RETIRED=True (L32) short-circuits search() before any network call (confirmed L157-160 in the module). No test file matches *jobsacuk* under tests/ (Glob + find both empty) — the retirement path itself is untested (only inferable via code reading). source_registry.py:267-287 `_jobsacuk` builder still imports the client, constructs it, and logs the retirement note every run for every project (cheap, but still an unconditional import+construct+log on every single daily_run regardless of country).
- **why debt:** This is the correctly self-gated case (KNOWN_ISSUES.md documents the retirement + rationale for keeping the module for a cheap future revival) — flagging for completeness per the audit's mandate, not because it needs fixing. The only real gap is that RETIRED itself is untested (a future accidental edit to RETIRED or the guard order could silently reintroduce live 404 polling for a UK project, since no test pins the invariant).
- **proposed fix:** Recommendation: leave as-is (self-gated, near-zero cost, revival path documented). Optional cheap addition: one regression test asserting `JobsAcUkClient(...).search(...)` makes zero network calls while RETIRED=True, to lock the invariant KNOWN_ISSUES.md already promises.

### 36. [us-first] Non-US metro matching has no dedicated test file despite being flagged as a known gap
- **files:** config.py, match/ (metro_variants + local-job classification), tests/test_s35_international.py
- **risk/effort:** low/L
- **evidence:** tests/test_s35_international.py:52-76 covers `test_metro_variants_adds_bare_city_for_non_us`, `test_international_local_job_classifies_local`, `test_us_local_job_still_local` — 3 of the file's 11 tests exercise this path, all in one shared international-grab-bag file rather than a dedicated metro/international test module. KNOWN_ISSUES.md L48-50 explicitly calls out '#27 Non-US metro scoring beyond substring' as an accepted, unresolved gap (bare-city fallback only, no CBSA-equivalent table for non-US metros), and again at L82-83 flags the 'Minneapolis, MN'-style hyphenated-multi-city gap.
- **why debt:** Owner already accepted this as a gap in writing (not a surprise finding) — reporting for completeness of the maintenance-surface map per the audit's brief, since it is genuinely international-only code with real but thin coverage (3 tests for the entire non-US local-matching heuristic) and zero coupling into the US path (bare substring fallback only fires when the US CBSA table has no entry, i.e. never for a US location).
- **proposed fix:** No action needed for US-first priority; already correctly triaged as low-priority/accepted in KNOWN_ISSUES.md. If ever revisited, build a non-US metro table analogous to the US CBSA data (owner's own suggested fix, already written down).

### 37. [us-first] Careerjet and Jooble country-routing (config.py CAREERJET_LOCALES / JOOBLE_COUNTRY_HOSTS) are cleanly self-gated — confirmed clean, no action
- **files:** config.py, search/careerjet_client.py, search/jooble_client.py, search/source_registry.py
- **risk/effort:** low/S
- **evidence:** config.py:284-299 `careerjet_locale_for('us')` and `jooble_host_for('us')` both explicitly short-circuit and return None/'jooble.org' — the exact same values used before country-routing was added (documented byte-identical-for-US in the surrounding comments, e.g. L256-262, L270-273). source_registry.py:184-209 `_jooble`/`_careerjet` builders pass `ctx.country` unconditionally but the underlying functions are no-ops for 'us'. tests/search/test_jooble.py (7 tests) and tests/search/test_careerjet.py (7 tests) both cover the country-host/locale swap.
- **why debt:** Not debt — reporting as a positive confirmation per the audit's "leave as-is" category, since the owner asked for a full inventory including clean cases.
- **proposed fix:** Leave as-is. No action.

### 38. [us-first] Adzuna country-tail stripping runs one extra dict-lookup on every US where-string (negligible, already justified) — confirmed clean, no action
- **files:** config.py, search/adzuna_client.py
- **risk/effort:** low/S
- **evidence:** search/adzuna_client.py:70-78 calls `config.location_country_tail(where)` unconditionally whenever `where` is truthy, even for US locations; config.py:227-236 the function is a cheap comma-split + dict lookup that returns None for any 'City, ST' US tail (state abbreviations deliberately excluded from `_COUNTRY_NAME_TO_CC`, per the comment at config.py:214-215), so the `if tail_cc and tail_cc == self.country` guard at adzuna_client.py:77 never fires for 'us'. Covered by tests/test_s35_international.py:90-131 (3 tests: strips-matching-tail, US-unchanged, foreign-tail-not-matching-routed-country-kept).
- **why debt:** Not debt — negligible fixed per-call cost (one dict lookup), already the byte-identical-for-US invariant that the surrounding comments promise and tests pin. Included in the audit for completeness.
- **proposed fix:** Leave as-is. No action.
